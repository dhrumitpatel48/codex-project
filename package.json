{
  "name": "professional-data-platform",
  "version": "1.0.0",
  "description": "Secure MVP platform for managing professional data with Firebase, Firestore, and Stripe payments.",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "lint": "eslint .",
    "test": "node --test",
    "check": "npm run lint && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "express-validator": "^7.2.0",
    "firebase-admin": "^12.7.0",
    "helmet": "^8.0.0",
    "node-cron": "^3.0.3",
    "stripe": "^17.3.0"
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "globals": "^15.11.0",
    "supertest": "^7.0.0"
  }
}
